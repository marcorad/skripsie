
file = "1";
desc = " (string ensemble)";

lr = importdata("../wav/moonlight " + file + ".wav").data;
s = size(lr(:, 1));
t = linspace(0, 1, s(1))';

subplot(2,1, 1);
plot(t, lr(:, 1));
title("Left channel");

subplot(2,1, 2);
plot(t, lr(:, 2));
title("Right channel");
xlabel("time [s]");
suptitle("Audio for file " + file + descr);

set(gcf, 'Position',[500,500, 450, 300]);




